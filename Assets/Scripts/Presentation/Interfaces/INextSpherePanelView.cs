namespace Presentation.Interfaces
{
    public interface INextItemPanelView
    {
        // Methods related to UI updates
        void CreateNextItemImages();
        void UpdateNextItemImages(int itemIndex);
    }
}
